<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Arbiter.App.ViewModels.Player"
             xmlns:controls="using:Arbiter.App.Controls"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="640"
             x:Class="Arbiter.App.Views.PlayerSkillbookView"
             x:DataType="vm:PlayerSkillbookViewModel">
    
    <Design.DataContext>
        <vm:DesignPlayerSkillbookViewModel/>
    </Design.DataContext>
    
    <UserControl.DataTemplates>
        <DataTemplate DataType="vm:PlayerSkillSlotViewModel">
            <Border Width="48" Height="48"
                            Classes="sprite-slot"
                            Background="Transparent"
                            BorderBrush="{StaticResource Talgonite.Border.HighlightBrush}"
                            BorderThickness="1"
                            ToolTip.Placement="Pointer"
                            IsEnabled="{Binding !IsEmpty}">
                        <controls:SpritePresenter Source="{Binding SpriteImage}"
                                                  SlotText="{Binding RelativeSlot}"
                                                  FallbackText="{Binding SpriteFallbackText}"
                                                  IsEmpty="{Binding IsEmpty}"
                                                  IsCooldownActive="{Binding HasCooldown}"
                                                  CooldownText="{Binding CooldownText}"/>
                        <!-- Tooltip -->
                        <ToolTip.Tip>
                            <Border Padding="4">
                                    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="*,Auto"
                                          RowSpacing="4" ColumnSpacing="8"
                                          MinWidth="200">
                                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                            <!-- Icon -->
                                            <controls:SpritePresenter Width="36" Height="36"
                                                                      Source="{Binding SpriteImage}"
                                                                      FallbackText="{Binding SpriteFallbackText}"
                                                                      IsEmpty="{Binding IsEmpty}"/>

                                            <!-- Name -->
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource Talgonite.TextBlock.Foreground}"
                                                       FontSize="16"
                                                       FontFamily="{StaticResource Talgonite.Font.InterfaceFont}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <!-- Slot -->
                                        <TextBlock Grid.Row="0" Grid.Column="1"
                                                   Text="{Binding RelativeSlot, StringFormat={}Slot {0}}"
                                                   TextAlignment="Right"
                                                   Foreground="{StaticResource Talgonite.TextBlock.DimForeground}"
                                                   FontSize="14"
                                                   FontFamily="{StaticResource Talgonite.Font.Monospace}"
                                                   VerticalAlignment="Center"/>
                                        
                                        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                Margin="-4,0"
                                                Classes="divider-h"/>
                                        
                                        <!-- Sprite Number -->
                                        <TextBlock Grid.Row="2" Grid.Column="0"
                                                   Text="{Binding Sprite, StringFormat={}Icon {0}}"
                                                   Foreground="{StaticResource Talgonite.TextBlock.Foreground}"
                                                   FontSize="14"
                                                   FontFamily="{StaticResource Talgonite.Font.Monospace}"
                                                   VerticalAlignment="Center"/>
                                        
                                        <!-- Level -->
                                        <TextBlock Grid.Row="2" Grid.Column="1" Foreground="{StaticResource Talgonite.Color.White}"
                                                   FontSize="14"
                                                   FontFamily="{StaticResource Talgonite.Font.Monospace}"
                                                   VerticalAlignment="Center"
                                                   IsVisible="{Binding HasLevel}">
                                            <Span>
                                                <Run Text="Level" Foreground="{StaticResource Talgonite.Color.LightGray}"/>
                                                <Run Text="{Binding CurrentLevel}"/>
                                                <Run Text="/"/>
                                                <Run Text="{Binding MaxLevel}"/>
                                            </Span>
                                        </TextBlock>
                                        
                                        <!-- Cooldown -->
                                        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Foreground="{StaticResource Talgonite.Color.LightBlue}"
                                                   FontSize="14"
                                                   FontFamily="{StaticResource Talgonite.Font.Monospace}"
                                                   VerticalAlignment="Center"
                                                   IsVisible="{Binding HasCooldownDuration}">
                                            <Span>
                                                <Run Text="Cooldown:"/>
                                                <Run Text="{Binding CooldownDurationText}"/>
                                            </Span>
                                        </TextBlock>
                                        
                                        <!-- Virtual Skill -->
                                        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Text="Virtual Skill"
                                                   Foreground="{StaticResource Talgonite.Color.LightYellow}"
                                                   FontSize="14"
                                                   FontFamily="{StaticResource Talgonite.Font.Monospace}"
                                                   VerticalAlignment="Center"
                                                   IsVisible="{Binding IsVirtual}"/>
                                    </Grid>
                                </Border>
                        </ToolTip.Tip>
                    </Border>
        </DataTemplate>
    </UserControl.DataTemplates>
    
    <Grid Background="{StaticResource Talgonite.Control.Background}"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <Grid Background="{StaticResource Talgonite.ListBox.Background}"
              Margin="2">
            <Border Classes="sunken-shadow" CornerRadius="0"/>
            <Border Classes="sunken-highlight" CornerRadius="0"/>
            
            <ScrollViewer IsVisible="{Binding Path=., Converter={x:Static ObjectConverters.IsNotNull}, FallbackValue={x:False}}">
                <StackPanel Margin="4">
                    <!-- Temuair Skills -->
                    <Label Content="Temuair"
                           FontSize="20"
                           FontWeight="Normal"
                           HorizontalAlignment="Center"
                           Margin="4,2"/>
                    <ListBox ItemsSource="{Binding TemuairSkills}"
                             SelectedValue="{Binding SelectedSkill}"
                             SelectionMode="Single"
                             Classes="borderless"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Margin="4,0">
                    
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="12" ColumnSpacing="8" RowSpacing="8"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                
                    <!-- Medenia Skills -->
                    <Label Content="Medenia"
                           FontSize="20"
                           FontWeight="Normal"
                           HorizontalAlignment="Center"
                           Margin="4,2"/>
                    <ListBox ItemsSource="{Binding MedeniaSkills}"
                             SelectedValue="{Binding SelectedSkill}"
                             SelectionMode="Single"
                             Classes="borderless"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Margin="4,0">
                    
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="12" ColumnSpacing="8" RowSpacing="8"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
            
                    <!-- World Skills -->
                    <Label Content="World"
                           FontSize="20"
                           FontWeight="Normal"
                           HorizontalAlignment="Center"
                           Margin="4,2"/>
                    <ListBox ItemsSource="{Binding WorldSkills}"
                             SelectedValue="{Binding SelectedSkill}"
                             SelectionMode="Single"
                             Classes="borderless"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Margin="4,0">
                    
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="6" ColumnSpacing="8" RowSpacing="8"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Placeholder Text -->
            <TextBlock Text="No Character Selected"
                       Classes="placeholder"
                       IsVisible="{Binding Path=., Converter={x:Static ObjectConverters.IsNull}, FallbackValue={x:False}}"/>
        </Grid>
    </Grid>
    
</UserControl>
